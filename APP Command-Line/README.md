## Usaremos mais de um pacote, então usar

```sh
go mod init app-command-line
```

## Dependência do pacote abaixo:

```sh
go get github.com/urfave/cli
```

## Buildar o código (Dentro da pasta do Go):

```sh
go build
```